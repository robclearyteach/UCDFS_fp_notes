
# file: story.txt
------------
initial line 
------------

$ git commit -m "initial commit"


$ git branch featureX
$ git branch featureY
$ git branch featureZ

$ git branch
  featureX
  featureY
  featureZ
* main




##
# 1.
#

$ git checkout featureX

# file: story.txt
------------
initial line 

featureX line 1		# add line
------------
git add story.txt
git commit -m "add featureX line 1"





##
# 2.
#

$ git checkout featureY

# file: story.txt
------------
initial line 

featureY line 1		# add line
------------
git add story.txt
git commit -m "add featureY line 1"




$ git checkout featureZ

# file: story.txt
------------
initial line 

featureZ line 1		# add line
------------
git add story.txt
git commit -m "add featureZ line 1"




$ git checkout main
Switched to branch 'main'

$ git checkout featureX
Switched to branch 'featureX'

$ git checkout featureY
Switched to branch 'featureY'



$ git log --oneline --all
4810bb7 (HEAD -> featureZ) add featureZ line1
2bdd939 (featureY) add featureY line 1
14e89ab (featureX) add featureX line 1
bf72706 (main) initial commit


$git checkout main
$ git merge --no-ff featureX -m "mergeX"
$ git tag -a v0.1 -m "version 0.1"

# [git tag: -a: annotated -> allows message: full object: better]



##
# no fastforward merge:
##
git merge --no-ff featureX -m "mergeX" means:

featureX = the branch being merged into the current branch (typically main).
--no-ff = do not use a fast-forward merge, even if Git could.




$ git merge --no-ff featureY

#
# resolve conflicts and save
#	'accept both conflicts'
#

$ git add story.txt
$ git commit -m "mergeY"
$ git tag -a v0.2 -m "version 0.2"

$ git log --oneline --all




$ git merge --no-ff featurez

#
# resolve conflicts and save
#	'accept both conflicts'
#

$ git add story.txt
$ git commit -m "mergeZ"
$ git tag -a v0.3 -m "version 0.3"

$ git log --oneline --all

$ git commit --allow-empty -m "Release v0.1"

git log --oneline --graph --all
* 6204b57 (HEAD -> main) Release v0.1
*   db4f066 (tag: v0.3) mergeZ
|\  
| * e1dd49c (featureZ) add featureZ line 1
* |   6946e40 (tag: v0.2) mergeY
|\ \  
| * | 7a6e739 (featureY) add featureY line 1
| |/  
* |   1c4aec7 (tag: v0.1) mergeX
|\ \  
| |/  
|/|   
| * ac5d882 (featureX) add featureX line 1
|/  
* e3a62cc initial commit



